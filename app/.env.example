VITE_DATA_SOURCE=mock
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# optional — defaults to Macarena Cibran Rosado if unset (see data/source/supabaseSchema.ts)
VITE_REP_EMAIL=
# optional — if set, shows a password screen before the app loads (client-side only, not real security)
VITE_APP_PASSWORD=
